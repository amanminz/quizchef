package io.quizchef.quiz.domain;

/**
 * Author-assigned difficulty of a question. Reserved as a future scoring
 * multiplier (PRD scoring rules).
 */
public enum Difficulty {
    EASY,
    MEDIUM,
    HARD
}
