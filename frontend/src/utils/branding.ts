/**
 * The deployment's display brand. Used wherever the app names itself at
 * runtime (the in-session browser-tab suffix); keep it in sync with the
 * static `<title>` and meta description in `index.html`, which cannot
 * import TypeScript.
 */
export const SITE_TITLE = "BELC Family Quiz Platform";
